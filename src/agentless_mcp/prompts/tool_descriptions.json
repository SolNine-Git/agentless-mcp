{
  "repo_map": "Rank the repository's files by relevance and render a token-budgeted skeleton of what matters. Start here; pass focus (files or symbol names) to seed the ranking toward a bug or feature. budget is in tokens (auto-sized by default); granularity is 'function' (default) or 'file'. Omitted arguments fall back to the repository's .agentless-mcp.json, then to built-in defaults (max_files 10).",
  "list_dir": "List the repository's files as a tree, honouring gitignore. For orientation when you need paths rather than symbols; depth and max_entries are capped.",
  "get_symbols_overview": "Render the named files as signatures with bodies elided -- the cheap first look at what a file declares. Escalate to expand_symbols for the few symbols you need rather than re-reading whole files.",
  "expand_symbols": "Return the full, line-numbered body of each named symbol. stable_ids come from repo_map, get_symbols_overview, find_symbol, or resolve_locations output (form: py:src/app/svc.py::Invoice.total). The escalation step after a skeleton view.",
  "read_slice": "Return numbered lines for 1-based inclusive [start, end] ranges in one file, with enclosing-scope headers. Line-level is the last resort; prefer expand_symbols when a symbol boundary fits.",
  "find_symbol": "Find symbols by substring or qualified name, optionally filtered by kind. Each hit carries file:line and a stable id usable with expand_symbols.",
  "find_referencing_symbols": "Find the symbols that reference a target, grouped by file -- fan-in for blast radius and error-path review. Every name match is still listed, but each file's group carries the evidence tier behind it (same-file, resolved-via-import, unique, name-only-ambiguous) so you can weigh the rows instead of trusting them equally. Set shared_callers to instead rank symbols whose callers overlap the target's (the 'does a utility for this already exist' check).",
  "resolve_locations": "Turn location strings (class:Invoice, function:Invoice.total, line:42) into merged line intervals and stable ids for one file. Unrecognized locations are reported individually, never dropped silently.",
  "capabilities": "Report loaded grammars and versions, cache state, and the caps in force. Call this when a language seems unsupported or results look stale.",
  "explain_symbol": "One card for a symbol: its definition site and signature, what it references (fan-out) and what references it (fan-in), each grouped by evidence tier, plus the import relationships of the file it lives in. Use it to place an unfamiliar symbol in a single call instead of chaining find_symbol and find_referencing_symbols. Rows resolved only by name list every candidate rather than picking one; target is a stable id or a qualified name.",
  "symbol_path": "The shortest chain of resolved relationships between two symbols, hop by hop, each hop carrying its relation, evidence tier and file:line. Use it when the question is how a change here could reach a failure there. Edges are walked in both directions and rendered with the direction they really run; name-only-ambiguous edges are excluded unless include_ambiguous is set, so a path never rests on a guessed binding. No path and an unknown endpoint are both answers with a message, never errors.",
  "import_cycles": "Every module-level import cycle in the repository, each rendered as a file -> file -> file chain that closes. Use it when an import error, a partially initialized module or a layering question needs the dependency knots named. Computed by strongly connected components over resolved imports; no cycles is an ordinary, successful answer."
}
