{
  "repo_map": "Where does this bug or feature live? The structural entry point: pass focus to seed the ranking toward the ask, and a token-budgeted skeleton of the most relevant files comes back. It is a top-N view rather than an enumeration: max_files defaults to 10, so ten files come back however large the repository is, and an auto-sized budget lands in a 2000-8000 token band, counted by the server's own chars/4 estimator and not by your model's tokenizer -- on this tool's id-dense output a real BPE count runs 10-35% higher, the band tests/unit/test_token_counter.py pins, so read budget as a reproducible knob rather than a promise about your bill. To orient in a repository you have not read yet, call analyze_structure(operation='communities') first for the file groups, then map into the group that matters. Routing rule: use grep when the literal string or file is already known; use this map when the target location is unknown or the change surface spans files. NOTE, WHY, HACK and TODO comments plus ADR/RFC citations appear below their enclosing symbol as linked rationale nodes. It ranks symbols, so it will not surface a file that matches your issue only as a string -- test fixtures, config and resource files, and modules reached by a setting name rather than a call edge. Pair it with a text search over the issue's distinctive literals rather than treating either pass as complete. A focus entry is a file path, a path suffix, a bare module stem ('envelope' for envelope.py), a qualified symbol name, or a bare function, method or class name matched exactly against the extracted symbols; each entry carries one vote split across the files it matched. Entries that match nothing are listed in unresolved_seeds and noted above the map -- if you see them, the ranking is not focused the way you asked. budget is in tokens (auto-sized by default); granularity is 'function' (default) or 'file'. Omitted arguments fall back to the repository's .agentless-mcp.json, then to the built-in defaults named above. At granularity 'function' each file's block opens with a stable ids: line naming that file's id pattern, and every symbol row below carries the qualified name alone; put a row's name where the pattern reads <QualifiedName> to build an id expand accepts. A file with no row under it prints no pattern, and granularity 'file' prints none at all. A position is an @line suffix, never an N| gutter -- the gutter means verbatim file content and a map row is a normalized signature.",
  "list_dir": "What exists in this repository? List the files as a tree, honouring gitignore. Pass an optional repository-relative path to render only that directory's subtree without hiding other tracked paths by policy. For orientation when you need paths rather than symbols; depth and max_entries are capped.",
  "get_symbols_overview": "What does this file declare? Render the named files as signatures with bodies elided -- the cheap first look, instead of reading the file. Each file's block opens with a stable ids: line naming the id pattern for that file (nested symbols qualify as Class.method), so an id for expand_symbols reads straight off the overview. Escalate to expand_symbols for the few symbols you need rather than re-reading whole files.",
  "expand_symbols": "You have located the symbols; now read their bodies. The escalation step after a skeleton view: returns the line-numbered body of each named symbol. stable_ids come from repo_map, get_symbols_overview, find_symbol, or resolve_locations output (form: py:src/app/svc.py::Invoice.total). Every requested id comes back: when the batch exceeds the output budget the longest bodies are cut to their leading lines, each marked with how many lines it shows out of how many, and a summary line names how many were shortened. To read a long body whole, expand that id on its own.",
  "read_slice": "You need exact lines and no symbol boundary fits: return numbered lines for explicit 1-based inclusive [start, end] ranges in one file, with enclosing-scope headers. The MCP call requires non-empty lines unless whole_file=true is explicitly set. A range starting beyond the file is reported with the file's true line count, never answered with substitute content; a range running past the end keeps its clamped tail. Line-level is the last resort; prefer expand_symbols when a symbol boundary fits.",
  "find_symbol": "Does this symbol exist, and where is it defined? Find symbols by substring or qualified name, optionally filtered by kind. Each hit carries file:line and a stable id usable with expand_symbols.",
  "find_referencing_symbols": "Who calls this symbol, and what breaks if it changes? Fan-in for blast radius and error-path review: the symbols that reference a target, grouped by file. Every name match is still listed, but each file's group carries the evidence tier behind it (class-scope, same-file, same-file member, resolved-via-import, unique, name-only-ambiguous) so you can weigh the rows instead of trusting them equally. Read the top four tiers as callers and the bottom two as candidates: 'unique' sounds strong and is not, because it means only that the repository defines that name exactly once, not that anything connects the two files. 'same-file member' is the method case: the repository defines the name once, in this file, so a call through any receiver here reaches it. No row is ever dropped for a weak tier. Set shared_callers to instead rank symbols whose callers overlap the target's (the 'does a utility for this already exist' check): the same limit bounds that listing with the overflow counted, each caller's vote is damped by its own fan-out so one promiscuous caller cannot flood the ranking, and candidates defined under a test tree are listed after every production candidate under a 'defined in tests' heading. Call it when the answer depends on who the callers are, not as a routine confirmation step: it is the most expensive tool here, and fan-in across a large repository can take minutes. Groups are ordered strongest tier first, and each is complete on its own: the header names the file and its tier, the stable ids: line directly beneath spells that file's id pattern in full, and each row carries the enclosing symbol's qualified name with its @line positions. Build an id by putting the row's name where the pattern reads <QualifiedName>; nothing has to be carried from elsewhere in the answer. A symbol referenced more than once in a file lists its positions on one row. A reference outside every symbol reads (module level) and carries no id, and a group of only those prints no pattern.",
  "explain_symbol": "What is this symbol and how is it wired in? Start here when you have one suspect symbol and have not seen it before. One call returns its definition site and signature, rationale comments linked to it, what it references (fan-out) and what references it (fan-in), each grouped by evidence tier, plus the import relationships of the file it lives in. Prefer it over find_symbol followed by find_referencing_symbols: the same answer in one round trip instead of two or three. Rows resolved only by name list every candidate rather than picking one; target is a stable id or a qualified name.",
  "analyze_structure": "Five whole-repository structure questions behind one operation argument: how are these two symbols connected (path), where are the import knots (cycles), which files belong together (communities), what does the module graph look like (diagram), what is unreferenced or central (health). operation='path' takes source and target (a stable id, a qualified name or a file path) and returns the fewest-hop chain of resolved relationships between them, each hop carrying its relation, evidence tier and file:line -- ask it when the question is how a change here could reach a failure there; only same-file and resolved-via-import edges participate by default. Repository-wide unique-name edges require include_unique and name-only-ambiguous edges require include_ambiguous, so the default path never rests on name-only binding evidence. operation='cycles' takes nothing and returns every module-level import cycle as a file -> file -> file chain that closes -- ask it when an import error, a partially initialized module or a layering question needs the knots named. operation='communities' takes an optional resolution (lower groups more coarsely) and returns which files belong together, each group labelled by the directory prefix most of its members share; modularity below 0.3 is marked as a weak partition to use only as a hint -- ask it to orient in an unfamiliar repository before reading any file. operation='diagram' takes optional focus, max_nodes and group_by_communities and returns a mermaid flowchart of the module graph, PageRank-bounded with an explicit elision marker; solid arrows are declared imports, dashed arrows are discounted name references, drawn only while the diagram stays legible and counted in a comment when left out -- ask for it only when a human will look at it, because the facts you should reason over are in the text views. operation='health' takes an optional limit and returns three readings of one degree count -- orphan candidates (a function no counted edge reaches or leaves), unused exports (a public function no counted edge reaches) and hubs (the highest total degree first); only same-file and resolved-via-import edges are counted, and a unique-name or name-only-ambiguous match is named on the row rather than counted, so an orphan row states the evidence it was not built on. No path, no cycles and an unknown endpoint are all ordinary answers with a message rather than errors; an unknown operation is rejected by the published enum.",
  "resolve_locations": "Which lines hold this class, function, or location? Turn location strings (class:Invoice, function:Invoice.total, line:42) into merged line intervals and stable ids for one file. Unrecognized locations are reported individually, never dropped silently.",
  "capabilities": "Why did this view stop short? Report the server version, loaded grammars and their warm state, cache generation state, the configured and client-advertised roots, and the caps in force. An absent tag cache is reported with the exact index command that builds it. Call this when a language seems unsupported, a generation mismatch persists, or repository selection did not pick the root you expected.",
  "orient": "Where does this bug or feature live, how is this repository shaped, how are two things connected -- start here. Six operations behind one operation parameter; there is no wire enum, so a wrong operation is answered with the valid list, and a parameter foreign to the selected operation is refused naming what that operation accepts and requires. operation='map' (budgeted skeleton, the structural entry point) takes focus, budget, limit and granularity: pass focus to seed the ranking toward the ask and a token-budgeted skeleton of the most relevant files comes back. It is a top-N view rather than an enumeration: limit defaults to 10, so ten files come back however large the repository is, and an auto-sized budget lands in a 2000-8000 token band, counted by the server's own chars/4 estimator and not by your model's tokenizer -- on this tool's id-dense output a real BPE count runs 10-35% higher, the band tests/unit/test_token_counter.py pins, so read budget as a reproducible knob rather than a promise about your bill. Use grep when the literal string or file is already known; use the map when the target location is unknown or the change surface spans files. It ranks symbols, so a file that matches only as a string -- fixtures, config, resources -- never surfaces; pair it with a text search over the issue's distinctive literals. A focus entry is a file path, a path suffix, a module stem, or a symbol name; entries matching nothing are listed as unresolved_seeds above the map. NOTE, WHY, HACK and TODO comments plus ADR/RFC citations appear below their enclosing symbol as linked rationale nodes. operation='communities' (cheap listing) takes resolution and limit: which files belong together -- call it first in a repository you have not read, then map into the group that matters. operation='cycles' (cheap listing) takes limit: every module-level import cycle. operation='diagram' (bounded mermaid, for a human to look at) takes focus, max_nodes, group_by_communities and resolution: the module graph as a flowchart; the facts to reason over are in the text views, and a diagram has one centre, so only the first focus entry is used. operation='path' (evidence-tier trace) requires source and target and takes include_unique and include_ambiguous: the fewest-hop chain of resolved relationships between two endpoints; the default path never rests on name-only evidence. operation='health' (code-health listing) takes limit: orphan candidates, unused exports and hubs in one answer, over the same resolved edge set path reads, with the degree counted on same-file and resolved-via-import edges alone and every weaker match named on the row it was discounted from -- ask it about dead code and architectural anchors, not about where a bug lives. Escalation chain: orient to locate, then symbols for declarations and bodies, then read for exact lines. At granularity 'function' each file's block opens with a stable ids: line naming that file's id pattern, and every symbol row below carries the qualified name alone; put a row's name where the pattern reads <QualifiedName> to build an id expand accepts. A file with no row under it prints no pattern, and granularity 'file' prints none at all. A position is an @line suffix, never an N| gutter -- the gutter means verbatim file content and a map row is a normalized signature.",
  "symbols": "You have a name, a file, or a stable id; get its declaration, body, or wiring. Five operations behind one operation parameter; there is no wire enum, so a wrong operation is answered with the valid list, and a parameter foreign to the selected operation is refused naming what that operation accepts and requires. operation='find' (cheap lookup) requires name and takes kind and limit: symbols by substring or qualified name, each hit carrying file:line and a stable id. operation='overview' (cheap skeleton) requires paths and takes docstrings: the named files as signatures with bodies elided -- the first look at what a file declares, instead of reading it; each file's block opens with a stable ids: line naming the id pattern (nested symbols qualify as Class.method). operation='expand' (budgeted bodies) requires stable_ids and takes limit: the line-numbered body of each named symbol -- the escalation step after a skeleton; when a batch exceeds the output budget the longest bodies are cut to their leading lines and marked, so expand a long id on its own. operation='explain' (one-call card) requires target and takes limit: definition site, signature, rationale comments linked to it, fan-out and fan-in grouped by evidence tier, plus the file's imports -- start here with one suspect symbol you have not seen; it replaces a find followed by a reference listing. operation='locate' (cheap resolution) requires path and locations and takes context_lines: turn class:/function:/line: strings into stable ids and merged intervals for one file; unrecognized locations are reported individually, never dropped. limit routes to the selected operation's own default when omitted. For who-calls-this and blast radius, use find_referencing_symbols -- deliberately its own tool, and the expensive one.",
  "read": "You need file contents rather than symbols: exact lines from one file, or the directory tree. Two operations behind one operation parameter; there is no wire enum, so a wrong operation is answered with the valid list, and a parameter foreign to the selected operation is refused naming what that operation accepts and requires. operation='slice' (bounded line read) requires path and takes lines, context_lines and whole_file: numbered lines for explicit 1-based inclusive [start, end] ranges with enclosing-scope headers; non-empty lines are required unless whole_file=true is explicitly set, a range starting beyond the file is reported with the file's true line count rather than answered with substitute content, and a range running past the end keeps its clamped tail. Line-level is the last resort: prefer symbols operation 'expand' when a symbol boundary fits. operation='dir' (cheap tree) takes path, depth and max_entries: the repository's files as a gitignore-honouring tree; pass path to render one subtree; depth and max_entries are capped."
}
