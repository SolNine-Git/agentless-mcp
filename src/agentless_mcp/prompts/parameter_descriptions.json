{
  "repo_root": "Absolute path of the repository to read. May be omitted when the server holds one repository, or when your client advertises a workspace root that selects exactly one; otherwise it is required, and a refusal lists the roots to choose from.",
  "map_focus": "File paths, path suffixes, module names, or symbol names that receive the map's ranking focus. A single string or a list of strings is accepted; a bare string reads as a one-element list. Use this when the target area is partly known; omit it for repository-wide orientation.",
  "map_budget": "Maximum approximate tokens for the rendered repository map. Omit it to use the repository configuration or automatic sizing.",
  "map_max_files": "Maximum ranked files admitted to the repository map before symbol packing.",
  "map_granularity": "Map detail: function lists symbols within ranked files; file reports ranked files only; body returns the file rows plus the top-scored symbols' whole bodies, budgeted -- one call in place of a map followed by an expand.",
  "no_cache": "Bypass the optional SQLite structural-fact cache for this call and parse every requested file live.",
  "tree_path": "Optional repository-relative directory whose tracked subtree should be rendered. Omit it to render from the repository root.",
  "tree_depth": "Maximum directory levels rendered below the selected tree root.",
  "tree_max_entries": "Maximum files and directories rendered before the tree reports truncation.",
  "overview_paths": "Repository-relative source files to render as signatures with bodies elided.",
  "docstrings": "Include symbol docstrings in overviews. Omit it to use repository configuration and then the built-in false default.",
  "stable_ids": "Stable symbol identifiers returned by the map, overview, find, explain, or location tools.",
  "expand_limit": "Maximum symbols expanded in this call; additional requested identifiers are reported as omitted. Omit it to use the tool's default.",
  "file_path": "Repository-relative path of the source file to read. The resolved path must remain beneath the authorized repository root.",
  "slice_lines": "One or more 1-based inclusive [start, end] line ranges. Required unless whole_file is explicitly true.",
  "context_lines": "Lines added before and after each requested or resolved interval.",
  "whole_file": "Explicitly request the entire bounded file when no line ranges are supplied. Cannot be combined with lines.",
  "find_name": "Symbol name, qualified name, or substring to search for.",
  "symbol_kind": "Optional exact symbol-kind filter for find_symbol.",
  "find_limit": "Maximum matching symbol cards returned by find_symbol. Omit it to use the tool's default.",
  "reference_target": "Stable id, qualified name, or symbol name whose referencing symbols should be found.",
  "reference_limit": "Maximum referencing-symbol groups returned after evidence-tier resolution. Omit it to use the tool's default.",
  "shared_callers": "Also rank symbols that repeatedly occur beside the target in the same callers; this is fuzzy retrieval evidence, not binding evidence.",
  "explain_target": "Stable id, qualified name, or symbol name to explain. Ambiguous names are refused with candidates.",
  "explain_limit": "Maximum rows shown within each fan-in, fan-out, and import section of the explanation. Omit it to use the tool's default.",
  "structure_operation": "Structural view to compute: path traces precise symbol evidence; cycles reports import SCCs; communities partitions the relevance graph; diagram emits bounded Mermaid; health reports orphan candidates, unused exports and hubs.",
  "path_source": "Starting stable id, qualified symbol name, or repository-relative file path for a path operation.",
  "path_target": "Ending stable id, qualified symbol name, or repository-relative file path for a path operation.",
  "include_unique": "Allow repository-wide unique-name edges in a path. Off by default because uniqueness is retrieval evidence, not import or same-file binding evidence.",
  "include_ambiguous": "Allow name-only ambiguous edges in a path. Off by default because each spelling has several candidate definitions.",
  "structure_limit": "Maximum cycles or communities returned, depending on the selected operation. Omit it to use the tool's default.",
  "community_resolution": "Positive modularity resolution for communities or community-grouped diagrams; larger values generally produce smaller communities.",
  "diagram_focus": "Optional file path, path suffix, module name, or symbol name at the centre of a diagram operation. A single string or a list of strings is accepted; a diagram has one centre, so only the first list entry is used.",
  "diagram_max_nodes": "Maximum real repository nodes drawn in a diagram before an elision node is added.",
  "diagram_max_edges": "Maximum arrows drawn in a diagram; reference edges past this bound are announced in a comment rather than drawn.",
  "group_by_communities": "Group diagram nodes into deterministic community subgraphs.",
  "locations": "Location selectors such as class:Name, function:name, or line:N to resolve within the named file.",
  "orient_operation": "Which orientation question to answer: map (budgeted skeleton of the most relevant files), communities (which files belong together), cycles (module-level import cycles), diagram (bounded mermaid module graph), path (fewest-hop evidence chain from source to target), or health (orphan candidates, unused exports and hubs). A wrong value is answered with this list; a parameter foreign to the selected operation is refused naming what it accepts and requires.",
  "symbols_operation": "Which symbol question to answer: find (lookup by name), overview (signatures with bodies elided for paths), expand (line-numbered bodies for stable_ids), explain (one symbol's wiring card for target), or locate (resolve class:/function:/line: strings within path). A wrong value is answered with this list; a parameter foreign to the selected operation is refused naming what it accepts and requires.",
  "read_operation": "Which read to perform: slice (numbered lines from one file) or dir (gitignore-honouring directory tree). A wrong value is answered with this list; a parameter foreign to the selected operation is refused naming what it accepts and requires.",
  "orient_focus": "File paths, path suffixes, module names, or symbol names that receive the ranking focus. A single string or a list of strings is accepted; a bare string reads as a one-element list. The map operation uses every entry; a diagram has one centre, so only the first entry is used there.",
  "orient_limit": "Maximum results for the selected operation: ranked files for map, communities for communities, cycles for cycles, rows per section for health. Omit it to use the selected operation's own default.",
  "symbols_limit": "Maximum result rows for the selected operation: matching cards for find, expanded symbols for expand, rows per section for explain. Omit it to use the selected operation's own default.",
  "read_path": "Repository-relative path the selected operation reads: for slice, the source file to read (required); for dir, an optional directory whose tracked subtree is rendered, defaulting to the repository root. The resolved path must remain beneath the authorized repository root."
}
