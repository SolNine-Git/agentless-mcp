{
  "repo_root": "Absolute path of the repository to read. Omit it when exactly one repository is selectable. A refusal lists the roots.",
  "map_focus": "File paths, path suffixes, module names, or symbol names that receive the map's ranking focus. A single string or a list of strings is accepted; a bare string reads as a one-element list. Use this when the target area is partly known; omit it for repository-wide orientation.",
  "map_budget": "Maximum approximate tokens for the map. Omit it for automatic sizing in a 2000-8000 token band.",
  "map_max_files": "Maximum ranked files admitted to the repository map before symbol packing.",
  "map_granularity": "Map detail: function lists symbols in ranked files, file lists files alone, body adds budgeted bodies. Body replaces a map followed by an expand.",
  "no_cache": "Parse every file live, bypassing the structural-fact cache.",
  "tree_path": "Optional repository-relative directory whose tracked subtree should be rendered. Omit it to render from the repository root.",
  "tree_depth": "Maximum directory levels below the tree root.",
  "tree_max_entries": "Maximum entries rendered before the tree reports truncation.",
  "overview_paths": "Repository-relative source files to render as signatures with bodies elided.",
  "docstrings": "Include symbol docstrings in overviews. Default false.",
  "stable_ids": "Stable symbol identifiers printed by map, overview, find, explain and locate.",
  "expand_limit": "Maximum symbols expanded in this call; additional requested identifiers are reported as omitted. Omit it to use the tool's default.",
  "file_path": "Repository-relative path of the source file, beneath the repository root.",
  "slice_lines": "One or more 1-based inclusive [start, end] line ranges. Required unless whole_file is true.",
  "context_lines": "Lines added before and after each requested or resolved interval.",
  "whole_file": "Return the whole bounded file. Use it only when lines is omitted.",
  "find_name": "Symbol name, qualified name, or substring to search for.",
  "symbol_kind": "Exact symbol-kind filter.",
  "find_limit": "Maximum matching symbol cards returned by find_symbol. Omit it to use the tool's default.",
  "reference_target": "Stable id, qualified name, or symbol name whose referencing symbols to find.",
  "reference_limit": "Maximum referencing-symbol groups returned. Default 50.",
  "shared_callers": "Rank symbols whose callers overlap the target's, instead of listing references. This is the does-a-utility-already-exist check, and fuzzy retrieval evidence, not binding evidence.",
  "explain_target": "Stable id, qualified name, or symbol name. An ambiguous name is refused with candidates.",
  "explain_limit": "Maximum rows shown within each fan-in, fan-out, and import section of the explanation. Omit it to use the tool's default.",
  "structure_operation": "Structural view to compute: path traces precise symbol evidence; cycles reports import SCCs; communities partitions the relevance graph; diagram emits bounded Mermaid; health reports orphan candidates, unused exports and hubs.",
  "path_source": "Path start: a stable id, qualified name, or file path.",
  "path_target": "Path end: a stable id, qualified name, or file path.",
  "include_unique": "Allow unique-name edges in a path. Off by default: a unique name is retrieval evidence, not binding evidence.",
  "include_ambiguous": "Allow name-only ambiguous edges in a path. Off by default: a spelling has several candidate definitions.",
  "structure_limit": "Maximum cycles or communities returned, depending on the selected operation. Omit it to use the tool's default.",
  "community_resolution": "Modularity resolution. Larger values generally give smaller communities.",
  "diagram_focus": "Optional file path, path suffix, module name, or symbol name at the centre of a diagram operation. A single string or a list of strings is accepted; a diagram has one centre, so only the first list entry is used.",
  "diagram_max_nodes": "Maximum repository nodes drawn before an elision node is added.",
  "diagram_max_edges": "Maximum arrows drawn. The rest are counted in a comment.",
  "group_by_communities": "Group diagram nodes into deterministic community subgraphs.",
  "locations": "Location selectors: class:Name, function:name, or line:N.",
  "orient_operation": "One of: map, communities, cycles, diagram, path, health. The tool description says what each one takes.",
  "symbols_operation": "One of: find, overview, expand, explain, locate. The tool description says what each one requires.",
  "read_operation": "One of: slice, dir. The tool description says what each one takes.",
  "orient_focus": "File paths, path suffixes, module stems, or symbol names that seed the ranking. Paste a traceback frame, blob URL, absolute path or path:line as it appears: it seeds the file it names. A diagram uses the first entry alone.",
  "orient_limit": "Maximum rows for the selected operation: files, communities, cycles or health rows. Health defaults to 20.",
  "symbols_limit": "Maximum rows for the selected operation. find and explain default to 20, expand to 10.",
  "read_path": "Repository-relative path: the file for slice, the subtree for dir. dir defaults to the repository root.",
  "history_target": "Stable id of the symbol, from a map, overview, find, explain or locate answer. A bare name is refused.",
  "history_limit": "Maximum commits returned. Default 10.",
  "history_budget": "Maximum approximate tokens spent on commit bodies. Default 12000."
}
